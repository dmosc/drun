[project]
name = "drun-e2e"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.50.0",
    "mcp>=1.0.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
