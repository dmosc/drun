[package]
name = "drun-integration-tests"
version = "0.1.0"
edition = "2024"
publish = false

[dev-dependencies]
drun-core = { path = "../../crates/drun-core" }
tempfile.workspace = true
toml = "0.8"
